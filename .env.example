# Same-origin API path (Vite proxies /api → Django in local dev; nginx does the same in production).
VITE_API_URL=/api

# Optional local override if you are not using the Vite proxy:
# VITE_API_URL=http://127.0.0.1:8011/api
